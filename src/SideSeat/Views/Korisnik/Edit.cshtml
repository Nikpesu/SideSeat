@model SideSeat.Models.ViewModels.KorisnikFormViewModel
@{
    ViewData["Title"] = "Uredi korisnika";
}

<section class="ss-page">
    <nav class="ss-breadcrumb" aria-label="Breadcrumb">
        <a asp-controller="Home" asp-action="Index">Dashboard</a>
        <span class="ss-breadcrumb-sep">/</span>
        <a asp-controller="Korisnik" asp-action="Index">Korisnici</a>
        <span class="ss-breadcrumb-sep">/</span>
        <span class="current" aria-current="page">Uredi korisnika</span>
    </nav>

    <div class="ss-page-head">
        <h1>Uredi korisnika</h1>
        <p>Azuriirajte osobne podatke, uloge i KYC informacije.</p>
    </div>

    <form asp-action="Edit" method="post">
        @Html.AntiForgeryToken()
        <input type="hidden" asp-for="Id" />
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <partial name="_Form" model="Model" />

        <div class="ss-actions">
            <button type="submit" class="ss-btn">Spremi promjene</button>
            <a class="ss-btn ss-btn-secondary" asp-action="Details" asp-route-id="@Model.Id">Odustani</a>
        </div>
    </form>
</section>

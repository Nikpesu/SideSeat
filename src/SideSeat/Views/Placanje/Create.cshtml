@model SideSeat.Models.ViewModels.PlacanjeFormViewModel
@{
    ViewData["Title"] = "Novo placanje";
}

<section class="ss-page">
    <nav class="ss-breadcrumb" aria-label="Breadcrumb">
        <a asp-controller="Home" asp-action="Index">Dashboard</a>
        <span class="ss-breadcrumb-sep">/</span>
        <a asp-controller="Placanje" asp-action="Index">Placanja</a>
        <span class="ss-breadcrumb-sep">/</span>
        <span class="current" aria-current="page">Novo placanje</span>
    </nav>

    <div class="ss-page-head">
        <h1>Novo placanje</h1>
        <p>Unesite podatke o novoj transakciji.</p>
    </div>

    <form asp-action="Create" method="post">
        @Html.AntiForgeryToken()
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <partial name="_Form" model="Model" />

        <div class="ss-actions">
            <button type="submit" class="ss-btn">Spremi</button>
            <a class="ss-btn ss-btn-secondary" asp-action="Index">Odustani</a>
        </div>
    </form>
</section>
